// change your db connection here
const config = {
  db: {
    host: "localhost",
    user: "root",
    password: "",
    database: "",
    connectTimeout: 60000
  },
  listPerPage: 10,
};

module.exports = config;